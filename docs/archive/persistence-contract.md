# Persistence Contract

Archived on `2026-03-07`.

Это historical persistence note из периода, когда save/load ещё считались будущей задачей.

Что он покрывал:

- pre-implementation contract thinking
- раннее разделение authoritative vs derived state
- минимальный состав сейва до реального runtime contract

Почему архивирован:

- проект давно пишет и читает реальные сейвы
- актуальный write format уже `SaveGameV3`
- current docs теперь используют `docs/persistence.md` как описание текущего состояния
