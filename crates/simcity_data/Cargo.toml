[package]
name = "simcity_data"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true }
rand = { workspace = true }
ron = { workspace = true }
serde = { workspace = true }
simcity_core = { path = "../simcity_core" }
simcity_sim = { path = "../simcity_sim" }

[dev-dependencies]
# Headless determinism test: sim-crate systems use EguiContexts, whose resources
# are normally provided by EguiPlugin (not added in tests).
bevy_egui = { workspace = true }
