[package]
name = "simcity_sim"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true }
bevy_egui = { workspace = true }
rand = { workspace = true }
ron = { workspace = true }
serde = { workspace = true }
simcity_core = { path = "../simcity_core" }

[features]
# Dev-only startup convenience: auto-enter InGame and load the test city. Off in a shipped
# build, where the main menu and the scenario catalogue are the real entry point.
dev = []
