# Фаза 7 — завершение: перф теней, day_night-конфиг, зачистка 2D-нейминга, docs sync

Контекст: миграция псевдо-3D завершена (фазы 1–6 ✅). Решения: `examples/pseudo3d_proto.rs` **оставить витриной** (правки света/камеры зеркалим), объём — **вся фаза 7**. Работа на ветке `test-zai`.

## 0. Инфраструктура замеров (без внешних MCP)

- Источник чисел — сама игра: `cargo run --release --features dev` поднимает BRP HTTP на `127.0.0.1:15702`; телеметрия (fps_smoothed, frame_time_smoothed_ms, population/buildings) — через `curl` → `bevy_debugger/debug_dump`; скриншоты — `bevy_debugger/screenshot` → PNG на диск. Из MCP — только computer-use (кнопки скорости ▶▶▶, визуальный контроль).
- День/ночь: X3 (6×) кнопкой в top-bar; замеры на фиксированных часах (полдень / глубокая ночь). Город — авто-загружаемый test city (детерминированный), «большой» = прогон на X3 до целевого населения.
- Прогрев ~3.5 с (Metal) до первого замера/скрина.
- Контрольная точка чистым `--release` без dev (FPS с HUD через computer-use) — убедиться, что dev-фича не искажает перф.
- Остановка: `pkill -f 'target/release/simcity'`; заодно расширить `stop_game.sh` на release-путь (сейчас матчит только `target/debug`).
- Если тюнинга каскадов не хватит — `--features dev,profile_chrome` (chrome-trace JSON без GUI); Tracy ставить через brew только по согласованию.

## 1. Перф: теневые каскады (главная)

Сейчас: `crates/simcity_frontend/src/game/camera.rs:102-117` — `maximum_distance: 900`, `first_cascade_far_bound: 500`, 4 каскада по 2048² (дефолты Bevy, `DirectionalLightShadowMap` не вставлен). Камера орто на 500, `far: 1000`. Замеры roadmap: день ~44 FPS.

Шаги:
1. База: release+dev билд, test city (обычный и «большой»), FPS+frame ms днём и ночью через debug_dump; скриншоты до.
2. Матрица (по одному, замер после каждого): A — `DirectionalLightShadowMap { size: 1024 }`; B — `num_cascades: 2–3` + `maximum_distance: ~600`; C — лучшая комбинация + подбор `first_cascade_far_bound`.
3. Приёмка: ≥55 FPS (цель 60) днём и ночью на большом городе; тени возле камеры зримо не деградировали (скриншот-сравнение).
4. Зеркалить итог в `examples/pseudo3d_proto.rs:238-250`; числа записать в roadmap/`performance-audit.md`.

## 2. `day_night.ron` — настоящий конфиг света

`assets/config/day_night.ron` = `(max_night_alpha: 0.55)` — наследие затемняющего квада; константы зашиты в `day_night.rs` (`SUN_DAY=12_000`, `AMBIENT_DAY=700`, emissive-силы).
1. Расширить `DayNightVisualConfig` (`crates/simcity_sim/src/game/day_night.rs:39-50`): sun/ambient/emissive-поля, переименовать `max_night_alpha`; дефолты = текущим константам (бит-в-бит).
2. Обновить ron + parse-тест в `simcity_data` config_loader.
3. Тюн значений через ron (без перекомпиляции), если замеры задачи 1 покажут нужду.

## 3. Зачистка 2D-нейминга (только комментарии/имена, поведение не меняется)

`glyphs.rs:1,4,15,58`; `public_transport.rs:34,66`; `traffic/parking.rs:41`; `traffic.rs:96`; `buildings/decay.rs:115,278,366` (переменная `sprite`); `intersections/render.rs:179`; `map/render.rs:20`.

## 4. Docs sync + панель биндов

- `shortcuts.rs`: добавить PgUp/PgDn (внутриигровая панель не знает бинд из 7fc2908).
- README: рендер в «Что уже реализовано» (Camera3d орто, Mesh3d+vertex colors, солнце/тени, day/night); «Управление» — PgUp/PgDn; описание F10-ручек (`camera_speed`, `zoom_speed`, `zoom_ease`, `rotate_speed`).
- `docs/architecture.md`: новая секция «Рендер» (камера-риг Q/E/Ctrl+drag/PgUp-PgDn, батчинг vertex colors, слои, день/ночь с гейтом по `City.hour`, тени).
- `docs/ui-architecture.md:541-599`: переписать устаревшую секцию камеры под текущий код (eased zoom c `zoom_ease`, yaw-aware пан `camera_speed*3.5*zoom_target`, clamp 0.05, `sync_dirty_tiles` → `map/render.rs`).
- Roadmap: фаза 7 — завершена; судьба прототипа — «витрина, правки зеркалим»; итоговые замеры.

## Верификация

Порядок 1→2→3→4 (docs последними). После каждой задачи: `cargo fmt --all` → `cargo clippy --all-targets --all-features -D warnings` → `cargo test --workspace` → коммит (Conventional Commits). Прототип компилируется в CI — после задачи 1 clippy и на нём.